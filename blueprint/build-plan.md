# Build plan (ordered feature checklist)

> Human-maintained. One workflow = one feature. Check items off when they ship
> (merged to `main`). To start the next item, spec it in `context/current-feature.md`.

- [x] **birthday-sync** — sync Google Contacts birthdays into a Google Calendar as
      yearly all-day events (idempotent). → `workflows/birthday-sync/`,
      archived spec: `history/features/birthday-sync.md`.
- [ ] _(next workflow — add it here, then run `/new-workflow`)_

## Backlog / ideas

- (add future business workflows here)
